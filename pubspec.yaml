name: redo_wallet_provider
description: Blockchain network providers — send transactions, query balances, interact with nodes.
version: 0.1.0
publish_to: none

# SPDX-License-Identifier: MIT

environment:
  sdk: ^3.10.4

dependencies:
  crypto: ^3.0.6
  fixnum: ^1.1.0
  http: ^1.3.0
  redo_wallet_core:
    path: ../redo_wallet_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
